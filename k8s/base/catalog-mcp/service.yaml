---
apiVersion: v1
kind: Service
metadata:
  name: catalog-mcp
  labels:
    app.kubernetes.io/name: catalog-mcp
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: bookstore-agents
spec:
  ports:
  - name: http
    port: 8101
    targetPort: http
  selector:
    app.kubernetes.io/name: catalog-mcp
